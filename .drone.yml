kind: pipeline
name: releasing 1.0 for tester

steps:
  - name: build
    image: node
    commands:
      - npm i
      - npm run build

trigger:
    branch:
    - release/1.0 # Can not use wildcard as "release/*"
